 <h1>投稿一覧</h1>
   <% @lists.each do |list| %>
   <!-- 見えなくていいから%に=はつかない -->
   <!-- eachとすることで、リストレコードの数だけ出力を繰り返してくれる -->
      <p>タイトル</p>
      <span>
        <%=link_to list.title, todolist_path(list.id) %>
        <!-- todolist_pathとは？ 多分ヘルパー。。。-->
      </span>
      <!-- 詳細ページへいくリンクを作成 -->

        <p>画像</p>
        <%= attachment_image_tag list, :image, :fill, 100, 100, format: 'jpeg' %>
   <% end %>